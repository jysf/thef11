---
layout: base.njk
title: crustyimg
---
{#
  A small sub-section on the tool this blog is built to exercise. Kept factual:
  everything here is true today (real CLI, real install, live browser demo).
  It will grow as the plugin and the pipeline land.
#}
<div class="wrap article">
  <header class="article__head">
    <p class="eyebrow">The tool behind the pictures</p>
    <h1>crustyimg</h1>
    <p class="lead">Outcome-driven image compression: you declare the result you want — a perceptual quality target — instead of guessing a quality number. One static Rust binary, zero system dependencies.</p>
    <p class="byline"><a href="https://github.com/jysf/crustyimg" rel="external">Open source on GitHub ↗</a></p>
  </header>

  <p>crustyimg is an open-source command-line tool for preparing images for the web. It auto-orients, strips location and identity metadata by default, and picks the smallest modern format — AVIF for photographs, lossless WebP or PNG for graphics — that still meets the quality you asked for. f/11 is its dogfooding testbed: the pipeline that brings photos into this blog runs on it.</p>

  <h2>Try it in your browser</h2>
  <p>The playground below runs the real Rust engine compiled to WebAssembly, so nothing is uploaded — drop a photo in and watch <code>crustyimg web</code> make it web-ready.</p>
</div>

{# The demo breaks out past the reading measure so the before/after panes have
   room. It's the reskinned playground itself, iframed — same engine as the CLI. #}
<div class="demo-embed" style="max-width: 64rem; margin-inline: auto; padding-inline: var(--gutter); margin-block: var(--step);">
  <iframe src="https://jysf.github.io/crustyimg/" title="crustyimg playground — make an image web-ready in your browser" loading="lazy" style="width: 100%; height: 780px; border: 1px solid var(--rule); background: var(--bg);"></iframe>
  <p class="byline mono"><a href="https://jysf.github.io/crustyimg/" rel="external">Open full-screen ↗</a></p>
</div>

<div class="wrap article">
  <h2>Install</h2>
  <pre><code>cargo install crustyimg                 # crates.io
brew install jysf/tap/crustyimg         # Homebrew
cargo install --git https://github.com/jysf/crustyimg</code></pre>
</div>
